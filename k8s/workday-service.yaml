apiVersion: v1
kind: Service
metadata:
  name: workday-service
  namespace: workday-app-ns
spec:
  type: NodePort
  selector:
    app: workday-api
  ports:
  - port: 9000
    targetPort: 8080
    nodePort: 30000
